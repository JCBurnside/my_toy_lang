for<T> let id a : T -> T = a

type Concrete = {
    a : int32
}

for<T> type Generic = {
    a : T
}

let takes_fn fun : (int32 -> int32) -> int32 = fun 3

let main _ : () -> () = 
    let test : Concrete = Concrete { a:0 }
    id test.a
    print_str "asdf"
    return ()